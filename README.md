# obsidian

Obsidian templates.  Put these files wherever you store your templates.

Use requires the following plugins:
- Templater
- Model Forms (ensure you create the necessary fields.  Note that anything using map() function requires a multi-select field type.)
- QuickAdd recommended for easily calling creation, e.g. "New Meeting", "New Person", etc.
